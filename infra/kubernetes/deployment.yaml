apiVersion: apps/v1
kind: Deployment
metadata:
  name: coreloom
  labels:
    app.kubernetes.io/name: coreloom
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: coreloom
  template:
    metadata:
      labels:
        app.kubernetes.io/name: coreloom
    spec:
      securityContext:
        runAsNonRoot: true
        fsGroup: 1001
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: app
          image: ghcr.io/moxxy-ai/coreloom:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: '3000'
            - name: CL_AUTH_DATABASE
              value: /data/auth.db
            - name: CL_AGENTS_DATABASE
              value: /data/agents.db
            - name: CL_WORKFLOWS_DATABASE
              value: /data/workflows.db
            - name: CL_AUTH_ALLOW_SIGN_UP
              value: 'false'
            - name: CL_AUTH_SECURE_COOKIE
              value: 'true'
            # agents.core refuses to boot in production without both keys.
            # See agents-secret.example.yaml for the Secret shape.
            - name: CL_AGENT_CREDENTIAL_KEY
              valueFrom:
                secretKeyRef:
                  name: coreloom-agents
                  key: credentialKey
            - name: CL_AGENT_RUN_GRANT_KEY
              valueFrom:
                secretKeyRef:
                  name: coreloom-agents
                  key: runGrantKey
            - name: CL_WORKFLOWS_PAYLOAD_KEY
              valueFrom:
                secretKeyRef:
                  name: coreloom-agents
                  key: workflowsPayloadKey
            - name: CL_WORKFLOWS_CURSOR_KEY
              valueFrom:
                secretKeyRef:
                  name: coreloom-agents
                  key: workflowsCursorKey
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: coreloom-data
              mountPath: /data
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 64Mi
        - name: coreloom-data
          persistentVolumeClaim:
            claimName: coreloom-data
