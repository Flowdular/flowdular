# Example only: not listed in kustomization.yaml. Create the real Secret
# out of band, for example:
#   kubectl create secret generic coreloom-agents \
#     --from-literal=credentialKey="$(openssl rand -base64 32)" \
#     --from-literal=runGrantKey="$(openssl rand -base64 32)" \
#     --from-literal=workflowsPayloadKey="$(openssl rand -base64 32)" \
#     --from-literal=workflowsCursorKey="$(openssl rand -base64 32)"
apiVersion: v1
kind: Secret
metadata:
  name: coreloom-agents
  labels:
    app.kubernetes.io/name: coreloom
type: Opaque
stringData:
  credentialKey: REPLACE_WITH_BASE64_32_BYTE_KEY
  runGrantKey: REPLACE_WITH_BASE64_32_BYTE_KEY
  workflowsPayloadKey: REPLACE_WITH_BASE64_32_BYTE_KEY
  workflowsCursorKey: REPLACE_WITH_BASE64_32_BYTE_KEY
