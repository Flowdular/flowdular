/* GENERATED by "coreloom module sync". Do not edit by hand; run
   "pnpm coreloom module sync --apply" after enabling or disabling modules. */
import type {
	ModuleClientContext,
	ModuleClientContribution,
} from '@coreloom/client';
import { createClientContribution as system_core } from '@coreloom/module-system/client';
import { createClientContribution as auth_core } from '@coreloom/module-auth/client';
import { createClientContribution as agents_core } from '@coreloom/module-agents/client';
import { createClientContribution as automations_core } from '@coreloom/module-automations/client';
import { createClientContribution as workflows_core } from '@coreloom/module-workflows/client';
import { createClientContribution as catalog_core } from '@coreloom/module-catalog/client';
import { createClientContribution as expenses_core } from '@coreloom/module-expenses/client';
import { createClientContribution as parties_core } from '@coreloom/module-parties/client';
import { createClientContribution as profile_core } from '@coreloom/module-profile/client';
import { createClientContribution as sandbox_core } from '@coreloom/module-sandbox/client';
import { createClientContribution as users_core } from '@coreloom/module-users/client';

export function createModuleClientContributions(
	context: ModuleClientContext,
): readonly ModuleClientContribution[] {
	return [
		system_core(context),
		auth_core(context),
		agents_core(context),
		automations_core(context),
		workflows_core(context),
		catalog_core(context),
		expenses_core(context),
		parties_core(context),
		profile_core(context),
		sandbox_core(context),
		users_core(context),
	];
}
