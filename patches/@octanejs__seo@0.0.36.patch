diff --git a/src/components.tsrx b/src/components.tsrx
index 4e9a5d0faab0bd72915ee7835e2bd79df4e2e9f6..03b978385a5e632f08b6f9411eef5ef65cbb1457 100644
--- a/src/components.tsrx
+++ b/src/components.tsrx
@@ -208,8 +208,10 @@ export function JsonLd(props: { data: unknown; seoKey?: string }) {
  */
 export function Seo(props: SeoInput) {
 	useRegisterSeo(expandSeo(props), {
-		site: props.site,
-		titleTemplate: props.titleTemplate,
+		...(props.site === undefined ? {} : { site: props.site }),
+		...(props.titleTemplate === undefined
+			? {}
+			: { titleTemplate: props.titleTemplate }),
 		declaredOpenGraph: props.openGraph !== undefined,
 		declaredTwitter: props.twitter !== undefined,
 	});
diff --git a/src/expand.ts b/src/expand.ts
index 4a1644ea4e681df7424b6fca5cf920c61fdb5199..362b4915c044170d556b85f188edd5c0d60699a4 100644
--- a/src/expand.ts
+++ b/src/expand.ts
@@ -148,7 +148,7 @@ export function expandSeo(input: SeoInput): SeoDescriptor[] {
 		// merge, so one place decides which origin applies.
 		const images = normalizeImages(og.images);
 		for (let i = 0; i < images.length; i++) {
-			const image = images[i];
+			const image = images[i]!;
 			const url = image.url;
 			// Repeated og:image tags are legitimate, so each gets its own identity.
 			out.push({
diff --git a/src/registry.ts b/src/registry.ts
index 66a781ec7b10bff3f45b2e662b1b746385610b6b..4f974f1d021aa9522bfe1898513c4587a0ba639d 100644
--- a/src/registry.ts
+++ b/src/registry.ts
@@ -38,8 +38,8 @@ export interface SeoRegistry {
 function sameDescriptors(a: readonly SeoDescriptor[], b: readonly SeoDescriptor[]): boolean {
 	if (a.length !== b.length) return false;
 	for (let i = 0; i < a.length; i++) {
-		const x = a[i];
-		const y = b[i];
+		const x = a[i]!;
+		const y = b[i]!;
 		if (x.key !== y.key || x.tag !== y.tag || x.text !== y.text) return false;
 		const xa = x.attrs;
 		const ya = y.attrs;
diff --git a/src/useStrayOwnerDiagnostic.ts b/src/useStrayOwnerDiagnostic.ts
index 95139d829579d61cb7f9165dc82e04799293cf72..6857b6c0cae80405c4acc0ff880d1ab8f40a9cf9 100644
--- a/src/useStrayOwnerDiagnostic.ts
+++ b/src/useStrayOwnerDiagnostic.ts
@@ -20,7 +20,10 @@ let liveOwners = 0;
 let reported = false;
 
 export function useStrayOwnerDiagnostic(owns: boolean): void {
-	if (process.env.NODE_ENV === 'production') return;
+	const processValue = (globalThis as {
+		readonly process?: { readonly env?: { readonly NODE_ENV?: string } };
+	}).process;
+	if (processValue?.env?.NODE_ENV === 'production') return;
 	useEffect(() => {
 		if (!owns) return;
 		liveOwners++;
