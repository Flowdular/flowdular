/** Shell views the Administration screens of system.core link between. */
export const SYSTEM_VIEWS = { modules: 'modules' } as const;

/** The audit trail auth.core contributes; the Flags tab sends a reviewer there. */
export const AUDIT_VIEW_ID = 'audit';

/** Action the settings runtime records for every flag change. */
export const FLAG_AUDIT_ACTION = 'settings.flag.changed';
